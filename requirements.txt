streamlit>=1.58.0  # 1.42 gave st.container(key=…); 1.5x gave st.iframe, which
                   # measures the component height itself. Without it the app
                   # falls back to a Python ESTIMATE and every section added
                   # widens the blank gap under the brief.
python-dotenv>=1.0.0
apify-client>=3.0.0
tiktoken>=0.7.0
pypdf>=4.0.0
certifi>=2024.0.0
anthropic>=0.34.0
youtube-transcript-api>=0.6.0
supabase>=2.3.0
pytrends>=4.9.0
plotly>=5.18.0
firecrawl-py>=1.0.0
